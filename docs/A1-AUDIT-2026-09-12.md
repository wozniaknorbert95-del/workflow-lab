# A1 Audit — AI-native readiness (2026-09-12)

**Prompt:** `workflow-marzen/03-PROMPTY-SZTABU.md` § A1  
**Auditor:** R1 (local execution) · **Scope:** read-only audit; fixes applied in PR for B4-T2..T4  
**Repo:** `wozniaknorbert95-del/workflow-lab` @ post-F3 main + this MR

## Scores (0–10)

| Area | Score | Notes |
| --- | --- | --- |
| AGENTS.md + commands work | **9/10** | §2 commands verified locally (`lint`, `test`, `build` all PASS). Missing: explicit pointer to 3 skills until this MR. |
| README / ARCHITECTURE / PRODUCT / DECISIONS | **8/10** | All exist; pre-MR ARCH/PRODUCT were thin vs F2 bar. **Fixed in this MR** (expanded one-pagers). |
| `.cursor/rules` vs AGENTS.md | **9/10** | Rules defer to AGENTS.md on conflict (`00-fundament.mdc`). Polish vs English docs — intentional. |
| CI parity (GitLab + GitHub) | **10/10** | `.gitlab-ci.yml` and `.github/workflows/ci.yml` run same `npm run` trio as `package.json`. |
| Clean checkout tests | **10/10** | Zero deps; Node 20+ sufficient. |
| Skills (repeatable procedures) | **6→9/10** | Was 1/3 (`review-bezpieczenstwa`). **Fixed:** `dodaj-test`, `dodaj-script`. |
| Cloud Agent infra | **9/10** | Dockerfile + OAuth documented in DECISIONS. Bugbot comments still PARTIAL (B4-T5). |

**Overall:** **8.7/10** → **9.2/10** after this MR (F2 memory layer).

## Verified commands (2026-09-12)

```
npm run lint   → PASS
npm test       → PASS (4 tests)
npm run build  → PASS → dist/hello.js
```

## Fixes applied (this MR, by priority)

| P | Finding | Fix |
| --- | --- | --- |
| P1 | ARCHITECTURE lacked directory map + “where to add feature” | Rewrote `ARCHITECTURE.md` |
| P1 | PRODUCT success criteria stale (pre-W-05 wording) | Rewrote `PRODUCT.md` with DoD/F3 state |
| P2 | F2 requires 3 skills; only 1 existed | Added `dodaj-test`, `dodaj-script` |
| P3 | No A1 audit artifact on file | This document |

## Deferred (not in this MR)

| P | Finding | Owner |
| --- | --- | --- |
| P2 | Bugbot enabled but zero PR comments | B4-T5 — code MR smoke |
| P3 | W-06 phone loop FAIL | Batch 7 |
| P3 | GitLab CE not installed | F0 Commander human-stop |

## Next A1

Re-run monthly or after stack change (trigger A2 if commands change).
