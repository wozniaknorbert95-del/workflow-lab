# Linear — 8 clicks (W-03)

Staff cannot create your account. After these clicks, W-03 can PASS. dsaas stays off this board (C7).

1. Open https://linear.app/signup (Free).
2. Workspace name: `quietforge-ops`.
3. Create project: `workflow-lab` (only this repo).
4. Label: `agent`.
5. Label: `review`.
6. Label: `blocked`.
7. Issue template — paste the 6 fields from `.github/ISSUE_TEMPLATE/agent-task.md` (Cel / Kontekst / Wymagania / Ograniczenia / Kryteria / Weryfikacja).
8. Tell staff: workspace URL. Staff writes `D-W3-LINEAR` dated in `DECISIONS.md`.

Until then: GitHub Issues + label `agent` on **this** repo only. Slack is optional and does not block DoD.

WIP: max 3 agent runs, max 5 In progress (`ops/workflow-marzen/04` in the academy repo).
