# Delivery loop (one page)

IDEA → issue (6 fields) → branch → `npm run lint && npm test && npm run build` → PR → CI → **auto-merge** (D-AUTOMERGE).

Never run this loop in `dsaas-platform-main`. Academy ▶ TERAZ points here.
